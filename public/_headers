# Video filenames stay stable across deploys. Skip `immutable` so a new
# encode at the same path is picked up after max-age.
/media/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
